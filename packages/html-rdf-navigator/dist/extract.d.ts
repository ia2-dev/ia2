import { type ExtractionResult } from "./model.js";
/** Extract the IA2 Core 0.1 RDF dataset represented by a document or fragment. */
export declare function extractDataset(root?: Document | DocumentFragment): ExtractionResult;
